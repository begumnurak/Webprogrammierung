<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Rezepte <%= category %></title>
        <link href="/dist/output.css" rel="stylesheet">
    </head>

    <main class="">
        <h1 class="font-bold text-2xl ">Rezepte <%= category %></h1>
        <p>Hier sind alle Rezepte der Kategorie <%= category %> aufgelistet.</p>
        <div id="recipes-container" class="flex flex-row flex-wrap justify-evenly">
            <% recipes.forEach(function(recipe) { %>
                <%- include('../partials/recipe', {recipe: recipe}); %>
            <% }); %>
        </div>
    </main>

</html>